#include<stdio.h>
int main()
{
    printf("enter the radius of circle");
    float side;
    scanf("%f",&side);
    float pi=3.14;
    printf("the area of the circle %f \n",pi*(side*side));
    return 0;
}
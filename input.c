#include<stdio.h>
int main()
{
    int age;
    printf("enter age");
    scanf("%d",&age);
    printf("age is %d \n",age);
    return 0;
}
/* %d is a format specifier d 
    %d is for integers 
    %f is for real numbers 
    %c is for characters
    '&' acts as an adress allotment nigga it allots the value which we take in scan f to the variable age*/